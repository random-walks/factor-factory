# DIAGNOSTICS CHECKLIST — {{ project }}

> Auto-generated by `factor_factory.jellycell.tearsheets.diagnostics`.
> Shows the diagnostic results pulled from `artifacts/*.json`.

## Project

`{{ project }}` — generated {{ generated_at }} via factor-factory v{{ version }}.

## Required diagnostics (v0.1 contract)

| # | Diagnostic | Status | Source |
|---|---|---|---|
| 1 | MultiIndex integrity | {{ status_or_pending('multi_index_assertions') }} | `multi_index_assertions(panel)` |
| 2 | Standardized mean differences | {{ status_or_pending('smd') }} | `standardized_mean_differences(panel, ...)` |
| 3 | Parallel-trends visual | {{ status_or_pending('parallel_trends') }} | `parallel_trends_plot(panel, ...)` |
| 4 | Residual diagnostics | {{ status_or_pending('residual_diagnostics') }} | `residual_diagnostics(residuals, ...)` |

{% if smd_table %}
## Standardized mean differences

{{ smd_table }}

Cutoff convention (Stuart 2010): `|SMD| > 0.1` flagged with `*`,
`|SMD| > 0.5` flagged with `***`.
{% endif %}

{% if residual_diagnostics_summary %}
## Residual diagnostics

| Test | Statistic | p-value | Interpretation |
|---|---|---|---|
| Jarque-Bera | {{ "%.3f"|format(residual_diagnostics_summary.jarque_bera_stat) }} | {{ "%.3f"|format(residual_diagnostics_summary.jarque_bera_p) }} | normality {{ "✓" if residual_diagnostics_summary.normality_passes else "✗" }} |
| Shapiro-Wilk | {{ "%.3f"|format(residual_diagnostics_summary.shapiro_wilk_stat) }} | {{ "%.3f"|format(residual_diagnostics_summary.shapiro_wilk_p) }} | (n={{ residual_diagnostics_summary.n }}) |
{% if residual_diagnostics_summary.breusch_pagan_p is not none %}
| Breusch-Pagan | {{ "%.3f"|format(residual_diagnostics_summary.breusch_pagan_stat) }} | {{ "%.3f"|format(residual_diagnostics_summary.breusch_pagan_p) }} | homoskedasticity {{ "✓" if residual_diagnostics_summary.homoskedasticity_passes else "✗" }} |
{% endif %}
{% endif %}

## Manual checks (edit me)

- [ ] Outcome distribution looks reasonable
- [ ] No leverage outliers driving the headline result
- [ ] Spillover-set sensitivity addressed
- [ ] Robustness to alternative panel binnings (`freq="QE"`, etc.)

<!-- tearsheet:freeze -->
<!-- Sections below this marker are not regenerated. -->

## Project-specific diagnostics

(Edit me — add anything unique to this analysis.)
