# FINDINGS — {{ project }}

> Auto-generated by `factor_factory.jellycell.tearsheets.findings`.
> Pulls headline numbers from `artifacts/*.json`.

## Project

`{{ project }}` — generated {{ generated_at }} via factor-factory v{{ version }}.

## Headline numbers

{% if headline_artifacts %}
{% for art in headline_artifacts %}
### `{{ art.name }}`

{% if art.caption %}*{{ art.caption }}*{% endif %}

```json
{{ art.preview }}
```
{% endfor %}
{% else %}
No headline artifacts found in `artifacts/`. Save them via
`jc.save(payload, "artifacts/<name>.json", caption="...")` and re-run
the renderer.
{% endif %}

## Estimation results

{% if did_results %}
| Method | ATT | SE | 95% CI | p-value | n |
|---|---|---|---|---|---|
{% for r in did_results %}
| `{{ r.method }}` | {{ "%.3f"|format(r.att) }} | {{ "%.3f"|format(r.se) }} | [{{ "%.3f"|format(r.ci_95_lower) }}, {{ "%.3f"|format(r.ci_95_upper) }}] | {{ "%.3f"|format(r.p_value) }} | {{ r.n }} |
{% endfor %}
{% else %}
No DiD results saved yet. Use:

```python
from factor_factory.engines.did import estimate
results = estimate(panel, methods=("twfe",))
jc.save(results.to_records(), "artifacts/did_results.json")
```
{% endif %}

## Figures

{% if figures %}
{% for fig in figures %}
- `{{ fig }}`
{% endfor %}
{% else %}
No figures registered yet.
{% endif %}

<!-- tearsheet:freeze -->
<!-- Sections below this marker are not regenerated. -->

## Interpretation

(Edit me — write the narrative around the numbers above.)
