# MANUSCRIPT — {{ project }}

> Stub generated by `factor_factory.jellycell.tearsheets.manuscript`.
> The manuscript is the long-form working-paper-style writeup. It is
> NOT regenerated wholesale on subsequent runs — sections below the
> freeze marker are preserved.

## Abstract

(One paragraph summary of the question, method, headline result, and caveat.)

## 1. Introduction

(Motivation: why this question matters, what's been done before, what's
new.)

## 2. Data

{% if panel_metadata %}
The analysis uses a panel at the **{{ panel_metadata.dimension }}** level,
{% if panel_metadata.freq %}binned to **{{ panel_metadata.freq }}** periods, {% endif %}with primary outcome
`{{ panel_metadata.outcome_cols[0] }}`{% if panel_metadata.outcome_cols|length > 1 %} (plus secondaries: {{ panel_metadata.outcome_cols[1:] | join(", ") }}){% endif %}. {% if panel_metadata.treatment_events %}Treatment events:
{% for ev in panel_metadata.treatment_events %}*{{ ev.name }}* ({{ ev.treated_units|length }} units, {{ ev.kind }}{% if ev.treatment_date %}, {{ ev.treatment_date }}{% endif %}){{ ", " if not loop.last else "." }}{% endfor %}{% endif %}
{% endif %}

## 3. Methodology

See [METHODOLOGY.md](METHODOLOGY.md) for the formal estimation
strategy + diagnostic approach.

## 4. Results

See [FINDINGS.md](FINDINGS.md) for the headline tables + figures.
This section narrates them.

## 5. Robustness

(Pre-trends, alternative engines, leverage diagnostics, MDE bounds.)

## 6. Discussion

(Interpretation, generalizability, limitations.)

## 7. Conclusion

(One paragraph.)

<!-- tearsheet:freeze -->
<!-- Sections below this marker are not regenerated. -->

## References

(Edit me.)

## Appendix

(Edit me.)
