# METHODOLOGY — {{ project }}

> Auto-generated by `factor_factory.jellycell.tearsheets.methodology`.
> Edit the prose freely; re-running the renderer preserves anything
> below the freeze marker near the bottom of this file.

## Project context

- **Project name:** `{{ project }}`
- **Generator:** `factor-factory v{{ version }}`
- **Generated at:** {{ generated_at }}

## Data

{% if data_files %}
Source files registered in the `data/` directory:

{% for file in data_files %}
- `{{ file }}`
{% endfor %}
{% else %}
No source files registered yet — drop them in `data/` and re-run.
{% endif %}

{% if record_count %}
Total records ingested: **{{ record_count }}**.
{% endif %}

## Panel construction

{% if panel_metadata %}
- **Cross-sectional dimension:** `{{ panel_metadata.dimension }}`
- **Period kind:** `{{ panel_metadata.period_kind }}`{% if panel_metadata.freq %} (freq=`{{ panel_metadata.freq }}`){% endif %}
- **Outcome column(s):** {{ panel_metadata.outcome_cols | join(", ") }}
{% if panel_metadata.weights_col %}
- **Weights column:** `{{ panel_metadata.weights_col }}`
{% endif %}
- **Treatment events:** {{ panel_metadata.treatment_events|length }}
{% if panel_metadata.treatment_events %}
{% for ev in panel_metadata.treatment_events %}
  - `{{ ev.name }}` ({{ ev.kind }}) — {{ ev.treated_units|length }} units treated{% if ev.treatment_date %} on {{ ev.treatment_date }}{% elif ev.period_value is not none %} at period_value={{ ev.period_value }}{% endif %}{% if ev.intensity is not none %}, intensity={{ ev.intensity }}{% endif %}{% if ev.arm %}, arm=`{{ ev.arm }}`{% endif %}
{% endfor %}
{% endif %}
{% else %}
Panel metadata not yet populated. Build the panel via
`factor_factory.tidy.Panel.from_records` and re-run the renderer.
{% endif %}

{% if panel_metadata and panel_metadata.provenance and panel_metadata.provenance.data_source %}
## Data provenance

{% set prov = panel_metadata.provenance %}
- **Source:** {{ prov.data_source }}
{% if prov.dataset_version %}- **Dataset version:** {{ prov.dataset_version }}{% endif %}
{% if prov.creator %}- **Creator:** {{ prov.creator }}{% endif %}
{% if prov.license %}- **License:** {{ prov.license }}{% endif %}
{% if prov.citation %}- **Citation:** {{ prov.citation }}{% endif %}
{% if prov.ethics_note %}- **Ethics note:** {{ prov.ethics_note }}{% endif %}
{% endif %}

## Estimation strategy

{% if engines %}
Engines run on this panel:

{% for engine in engines %}
- **{{ engine.method }}** — see `factor_factory.engines.{{ engine.family }}.{{ engine.method }}`.
{% endfor %}
{% else %}
No engines run yet. Call `factor_factory.engines.<family>.estimate(panel, ...)`
in the analysis notebook to populate this section.
{% endif %}

## Diagnostics

The four required-for-v0.1 diagnostics from
`factor_factory.diagnostics`:

1. `multi_index_assertions(panel)` — structural-integrity check.
2. `standardized_mean_differences(panel, ...)` — covariate balance.
3. `parallel_trends_plot(panel, ...)` — visual pre-trend check.
4. `residual_diagnostics(residuals, ...)` — Jarque-Bera + Shapiro-Wilk
   (+ Breusch-Pagan when `fitted_values=` is supplied).

See `DIAGNOSTICS_CHECKLIST.md` for the full diagnostics audit.

## References

- factor-factory documentation: <https://github.com/random-walks/factor-factory>
- jellycell documentation: <https://jellycell.readthedocs.io/>

<!-- tearsheet:freeze -->
<!-- Sections below this marker are not regenerated. Edit freely. -->

## Limitations

(Edit me — list any known caveats specific to this project.)
